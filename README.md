file reuqires up to date metadata

### Run Protocol 

* go to ```pull_entropy_metadata.sh``` and update the url
* run ```pull_entropy_metadata.sh``` to update metadata
* update main.rs as needed
* ```cargo run```